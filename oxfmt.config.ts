import { defineConfig } from 'oxfmt';

export default defineConfig({
  printWidth: 120,
  proseWrap: 'always',
  singleQuote: true,
  trailingComma: 'all',
  experimentalSortPackageJson: false,
  ignorePatterns: [],
});
